# sample.yml
name: Sample Workflow
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "This is a sample workflow."
